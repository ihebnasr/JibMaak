{% extends 'base.html.twig' %}

{% block title %}Categories index{% endblock %}

{% block body %}
    <h1>Categories index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Idcate</th>
                <th>DesignatioCate</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for category in categories %}
            <tr>
                <td>{{ category.idcate }}</td>
                <td>{{ category.designatioCate }}</td>
                <td>
                    <a href="{{ path('categories_show', {'idcate': category.idcate}) }}">show</a>
                    <a href="{{ path('categories_edit', {'idcate': category.idcate}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('categories_new') }}">Create new</a>
{% endblock %}
